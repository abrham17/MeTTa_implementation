;Take the path algorithm presented for the knight’s tour problem in the
;text. Rewrite the path call of the recursive code given in Section 3.1 to
;the following form:

;path(X, Y) :- path(X, W), move(W, Y).
;Examine the trace of this execution and describe what is happening.